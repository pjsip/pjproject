// swift-tools-version:5.9
// Generated by verify-xcframework.sh; the binary target is resolved from the
// local build rather than a release URL, and the linker settings are copied
// verbatim from build/apple/Package.swift.in so that the shipped manifest is
// what gets exercised.
import PackageDescription

let package = Package(
    name: "VerifyPJSIP",
    platforms: [.macOS(.v11)],
    targets: [
        .binaryTarget(name: "PJSIPBinary", path: "@XCFRAMEWORK@"),
        .target(
            name: "PJSIPLinkerSettings",
            dependencies: ["PJSIPBinary"],
            path: "Shim",
            linkerSettings: [
@LINKER_SETTINGS@
            ]
        ),
        .executableTarget(name: "App", dependencies: ["PJSIPBinary", "PJSIPLinkerSettings"]),
    ]
)
