Local changes:
1. Error build "Platform not recognized"
--- /libsrtp-2.5.0/crypto/include/datatypes.h
+++ /srtp/crypto/include/datatypes.h
@@ -58,8 +58,8 @@
 #include <netinet/in.h>
 #elif defined HAVE_WINSOCK2_H
 #include <winsock2.h>
-#else
-#error "Platform not recognized"
+// #else
+// #error "Platform not recognized"
 #endif

 #ifdef __cplusplus

2. Error in Visual Studio : "error C2146: syntax error: missing ')' before identifier 'PRIx64'"
--- a/libsrtp-2.5.0/srtp/srtp.c
+++ b/third_party/srtp/srtp/srtp.c
@@ -67,6 +67,8 @@
 #include <winsock2.h>
 #endif

+#include <inttypes.h>
+
 /* the debug module for srtp */
 srtp_debug_module_t mod_srtp = {
     0,     /* debugging is off by default */
